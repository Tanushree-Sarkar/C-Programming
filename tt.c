#include<stdio.h>
#include<string.h>
void main()
{
    char s[50];
    int i,s,l=0,w=0;
    printf("Enter Your String:");
    gets(s);
    wh
    for (i=s-1;i>0; i--)
    {

        if(a[i]==' ')
        break;
        t++;
	}
    printf("Length of the last word is %d",t);
}
